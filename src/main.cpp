#include "Life.hpp"
#include <iostream>
#include <vector>
#include <iterator>

int main() {
	Life data;

	// data.set_node(Coords{-1, -1}, 0, 0, ALIVE, CURRENT);
	// data.set_node(Coords{-1, -1}, 1, 1, ALIVE, CURRENT);
	// data.set_node(Coords{-1, -1}, 2, 2, ALIVE, CURRENT);
	// data.set_node(Coords{-1, -1}, 3, 3, ALIVE, CURRENT);
	// data.set_node(Coords{0, -1}, 1, 0, ALIVE, CURRENT);
	// data.set_node(Coords{-1, 0}, 2, 0, ALIVE, CURRENT);
	// data.set_node(Coords{0, 0}, 3, 0, ALIVE, CURRENT);
	// data.display_grid();
	// std::cout << '\n';
	// data.set_node(Coords{0, 0}, 3, 0, ALIVE, NEXT);
	// data.go_next();
	// data.display_grid();
}
